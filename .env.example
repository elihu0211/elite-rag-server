# Elite RAG Server - 環境變數範例
# 複製此檔案為 .env 並填入實際值

# =============================================================================
# 資料庫設定 (必填)
# =============================================================================
# PostgreSQL 連線字串，需安裝 pgvector 擴展
DATABASE_URL=postgresql+asyncpg://dev:12345678@localhost:5432/elite_rag

# =============================================================================
# 認證設定 (必填)
# =============================================================================
# JWT 簽名密鑰 - 生產環境請使用強隨機字串
# 產生方式: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=請更換為安全的密鑰

# =============================================================================
# 應用程式設定 (選填)
# =============================================================================
# 除錯模式 - 生產環境請設為 false
DEBUG=true

# CORS 允許來源 (JSON 陣列格式)
# CORS_ORIGINS=["http://localhost:3000"]

# =============================================================================
# 嵌入服務設定 (選填)
# =============================================================================
# 提供者: "local" (預設，使用 sentence-transformers) 或 "openai"
# EMBEDDING_PROVIDER=local

# 本地模型設定
# EMBEDDING_MODEL=all-MiniLM-L6-v2
# EMBEDDING_DIMENSION=384

# OpenAI 設定 (僅當 EMBEDDING_PROVIDER=openai 時需要)
# OPENAI_API_KEY=sk-你的OpenAI金鑰
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# =============================================================================
# 搜尋設定 (選填)
# =============================================================================
# 文本分塊大小 (字元數)
# CHUNK_SIZE=500

# 分塊重疊大小 (字元數)
# CHUNK_OVERLAP=50

# 預設搜尋結果數量
# DEFAULT_SEARCH_LIMIT=10

# 相似度閾值 (0-1，越高越嚴格)
# SIMILARITY_THRESHOLD=0.7

# =============================================================================
# 種子資料設定 (選填)
# =============================================================================
# 預設管理員帳號 - 首次啟動時自動建立
# ABP 對比：類似 IdentityDataSeedOptions

# 管理員 Email
SEED_ADMIN_EMAIL=admin@example.com

# 管理員密碼 - 生產環境請務必修改！
SEED_ADMIN_PASSWORD=Admin@123456

# 管理員名稱
SEED_ADMIN_NAME=System Administrator
